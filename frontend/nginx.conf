server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  client_max_body_size 21m;
  location /api/ { proxy_pass http://backend:8000; proxy_set_header Host $host; proxy_read_timeout 180s; }
  location / { try_files $uri /index.html; }
}
